import java.util.Arrays;
import java.util.Dictionary;

//This was just a test class
public class Runner {
    public static void main(String[] args) {
       // MD5 m = new MD5();
       // BruteForce b = new BruteForce ("-M", m.MD5("aaaaaaaaa"));
        //SHA256 m = new SHA256();
        //BruteForce b = new BruteForce ("-S", m.SHA256Hash("0#"));
        //BCryptHash m = new BCryptHash("-B");
        //BruteForce b = new BruteForce ("-B", (m.BCryptHash("aaa")));

    }


    }

